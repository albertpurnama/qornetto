# Qornetto 

Is a discord bot that ocassionally joins in the conversation on Discord.

To run:

```bash
python bot/main.py
```

## DB Migration

`alembic revision --autogenerate -m "Initial migration"` to generate a migration file.

`alembic upgrade head` to apply the migration.

## Deployment
